[mysqld]
bind-address         = %(private_ip)s

server-id            = 1
log_bin              = /var/log/mysql/mysql-bin.log
expire_logs_days     = 10
max_binlog_size      = 100M
binlog_do_db         = %(db_name)s

